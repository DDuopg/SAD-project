package com.example.demo.model;

public class Parent extends Participant{
    // Constructors
    public Parent() {}

    public Parent(String name, String account, String password) {
        super( name, account, password );
    }
}